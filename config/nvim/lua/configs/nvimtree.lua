local options = {
    view = {
        width = 25,
    },
    git = {
        enable = true,
        ignore = false,
        timeout = 500,
    },
    filters = {
        dotfiles = false,
        git_ignored = true,
    },
}

return options
